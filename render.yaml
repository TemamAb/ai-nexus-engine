services:
  - type: web
    name: ainexus
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python render_deploy.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
