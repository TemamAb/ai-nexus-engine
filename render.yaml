services:
  - type: web
    name: ainexus
    env: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: python deploy8001.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
