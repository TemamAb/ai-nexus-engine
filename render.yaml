services:
  - type: web
    name: ai-nexus-engine
    env: python
    plan: free
    buildCommand: pip install --only-binary=all --no-deps -r requirements.txt
    startCommand: python src/main.py
    envVars:
      - key: PORT
        value: 8000
